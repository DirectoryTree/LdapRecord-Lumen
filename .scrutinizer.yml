filter:
    excluded_paths:
        - tests/*
build:
    nodes:
        analysis:
            environment:
                php:
                    version: 7.2.13
            tests:
                override:
                    - command: php-scrutinizer-run
